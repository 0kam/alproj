# Pinned dependencies for reproducible development environment
# Requires Python 3.9-3.12
# For installation as a library, use: pip install git+https://github.com/0kam/alproj
#
# Optional: For advanced image matching methods (RoMa, LoFTR, LightGlue, etc.),
# install with: pip install "alproj[vismatch] @ git+https://github.com/0kam/alproj"

numpy==2.4.1
pandas==3.0.0
rasterio==1.5.0
opencv-python==4.13.0.90
moderngl==5.12.0
glcontext==3.0.0
cmaes==0.12.0
tqdm==4.67.1
scipy==1.17.0
